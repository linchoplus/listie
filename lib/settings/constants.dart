import 'package:flutter/material.dart';

// Colors
const kMainColor = Colors.blue;
const kSecondaryColor = Colors.blueGrey;

const kCheckBoxColor = Colors.blue;

// Type of letter
const kLetterTypeTitle = 'Courgette';
const kLetterTypeSubtitle = 'Courgette';

// Hive taskBox name
const kTaskBoxName = 'tasks';

// Border radius (corners)
const kBorderRadius = BorderRadius.only(
  topLeft: Radius.circular(20.0),
  topRight: Radius.circular(20.0),
);
